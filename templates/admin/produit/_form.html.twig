{% form_theme form 'bootstrap_5_layout.html.twig' %}
{{ form_start(form, { attr: { style: 'width: 40%;' } }) }}
    {{ form_widget(form) }}
    <button class="btn btn-outline-primary mt-2">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
